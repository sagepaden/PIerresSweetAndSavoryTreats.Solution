@{
  Layout = "_Layout";
}

@model PSST.Models.Treat

<h2>Are you sure you want to delete this tasty treat?</h2>

@Html.DisplayNameFor(model => model.TreatDescription): @Html.DisplayFor(model => model.TreatDescription)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn btn-primary btn-sm" />
}
@Html.ActionLink("Back to Treats", "Index")