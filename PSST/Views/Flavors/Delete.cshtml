@{
  Layout = "_Layout";
}

@model PSST.Models.Flavor

<h2>Are you sure you want to delete this?</h2>

@Html.DisplayNameFor(model => model.FlavorName): @Html.DisplayFor(model => model.FlavorName)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" id="deleteDetails"/>
}
<hr><br>
@Html.ActionLink("Back to List", "Index", "Flavor", null, new { @class = "btn btn-light" })